# Реальный конфиг с БД-паролем и API-ключами — используйте config/config.example.php как шаблон
config/config.php

# Загруженные фото блюд/логотип — runtime-данные, не часть кода
uploads/dishes/*
!uploads/dishes/.htaccess

# Локальные настройки Claude Code (специфичны для этой машины)
.claude/

# SSH-ключи никогда не должны попадать в репозиторий
ssh/
*.pem
id_rsa
id_ed25519
id_rsa.pub
id_ed25519.pub

# Системные файлы
.DS_Store
Thumbs.db
*.log
